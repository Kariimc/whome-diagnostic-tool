"""Flet UI controls for the WHome Diagnostic Tool.

UI modules may import from ``core`` but ``core`` never imports from ``ui`` —
this one-way dependency keeps the project free of circular imports.
"""
