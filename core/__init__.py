"""Core business logic for the WHome Diagnostic Tool.

This package contains *no* Flet/UI imports so that the dependency arrow always
points one way (ui -> core), which keeps the design free of circular imports.
"""
